{
  "name": "claude-mobile-proxy",
  "version": "1.0.0",
  "description": "Proxy for Claude Mobile Flutter app → cc.freemodel.dev",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
